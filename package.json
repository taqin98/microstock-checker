{
  "name": "microstock-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Microstock asset validator for SVG, EPS, and JPG files",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,worker,frontend -c cyan,yellow,magenta \"npm run dev -w backend\" \"npm run dev:worker -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
